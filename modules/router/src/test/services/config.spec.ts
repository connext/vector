describe.skip("config.ts", () => {
  describe("getRebalanceProfile", () => {
    it("should fail if rebalance profile does not exist", async () => {});
    it("should return the rebalance profile for given chain/asset", async () => {});
  });
});
