describe.skip("swap.ts", () => {
  describe("getSwappedAmount", () => {
    it("should fail if it cannot find a swap or an inverted swap", async () => {});
    it("should fail if it cannot calculate swap");
    it("should fail if the swap is not hardcoded");
    it("should work if not inverted + hardcoded rate", async () => {});
    it("should work if inverted + hardcoded rate", async () => {});
  });
});
