describe("CMCWithdraw.sol", () => {
  it.skip("should work", async () => {
    // should increase the total transferred amount
    // should transfer to recipients with given asset
  });
  it.skip("should fail if the tx has already been executed", async () => {});
  it.skip("should fail if alice signature is invalid", async () => {});
  it.skip("should fail if bob signature is invalid", async () => {});
  it.skip("should fail if transfer fails", async () => {});
});
