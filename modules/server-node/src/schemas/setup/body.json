{
  "title": "SetupBodySchema",
  "type": "object",
  "required": ["counterpartyIdentifier", "chainId", "timeout"],
  "properties": {
    "counterpartyIdentifier": { "type": "string" },
    "chainId": { "type": "number" },
    "timeout": { "type": "string" }
  }
}
