import { expect } from "chai";

import { VectorEngine } from "./index";

describe("VectorEngine", () => {
  it("should be created successfully", async () => {
    expect(true).to.be.ok;
    expect(VectorEngine).to.be.ok;
  });
});
